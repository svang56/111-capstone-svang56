//Author: Sunny Vang
#include<string>
//this will be the base class for the 3 fighting areas / will make own 
//class for the town itself

class fighting
{
    private:
        std::string blackKnight;
        std::string guard
        std::
        
    public:
        
        void encounterMonster(string);//this will randomly 
        bool fightOrRun(int);//if 1 then fight, if 2 then run
        void generateMonster(string, string, string);//th
        bool randomChance(string);//this will determine win chance for monster
        bool stayOrGo(int);//asks if want to fight again or leave
        
        //to get monster names, initialize within the do-while loop 
        //in the int main() into the constructor 

        //Each class inheriting will have their own constructor
        //and will have private variables of each monster

}
